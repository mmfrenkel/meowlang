Main()
{
        int count;
        int index;
        char * condition;
        count = 0;
        for ( count++ count < 10) {
                        condition = "count is still not 10";
        }
        count = 20;
        for ( count-- count > 10) {
                        {
        condition = "is count still more than 10?";
        condition = "count is still more than 10";
        }
        }
        for (index = 15 index-- index > 10) {
                        condition = "index is still more than 10";
        }
}